{
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"functions": {
		"api/**/*.py": {
			"includeFiles": "prisma/**"
		}
	},
	"installCommand": "python -m pip install -r requirements.txt && python -m prisma generate"
}
