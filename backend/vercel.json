{
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"functions": {
		"api/**/*.py": {
			"includeFiles": "prisma/**"
		}
	},
	"installCommand": "pip install -r requirements.txt && python -m prisma generate --schema ./prisma/schema.prisma",
	"buildCommand": "true"
}
